void main(){
    int x;
    int y;

    x = ReadInteger();
    y = 0;
    while (x > 10){
        x = x - 5;
        if (x != 10)
            continue;
        y = x;
    }

    Print(y);
}
